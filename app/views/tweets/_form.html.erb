
  <%= form_with(model: tweet, local: true) do |form| %>
    <% if tweet.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

        <ul>
        <% tweet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <%= form.text_area :content, :maxlength =>  140, :rows => 3, class: "input-tweet card-text content-tweet my-4" %>
    <div class="actions new-tweet">
      <%= form.submit "New tweet", class:"btn btn-primary px-4 py-2" %>
    </div>
  <% end %>
